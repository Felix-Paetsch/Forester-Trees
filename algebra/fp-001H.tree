\taxon{lemma}
\author{felixpätsch}
\date{2025-01-19}
\import{base-macros}

\p{If #{f : A \rightarrow B} is a horizontal arrow in a [double complex](fp-001C) then the natural induced map between vertical homolory objects.
#{A_{ver} \rightarrow B_{ver}} factors as}

##{
    A_{ver} \rightarrow A_\square \rightarrow ^\square B \rightarrow B_{ver}
}

\p{Similarly for vertical maps #{f : A \rightarrow B}.}